{
  "dependencies": {
    "flowbite-react": "^0.10.2",
    "moment": "^2.30.1",
    "tailwindcss": "^3.4.14"
  },
  "name": "nodereact-shop",
  "scripts": {
    "build": "npm install --include=dev && npm install --include=dev --prefix client && npm run build --prefix client && npm install --prefix api",
    "start": "node api/index.js"
  }
}
